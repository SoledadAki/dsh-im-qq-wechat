# Changelog

## 0.5.5

### 安全

- 配对码从 6 位数字改为 128 位随机串，新增有效期、错误次数上限、用尽后自动轮换与常量时间比较。此前任何能给机器人发消息的人都可以在无过期、无次数限制的情况下爆破配对码，命中即被持久化为 owner，再经 `/work` 与 `/safe danger-full-access` 在宿主上执行任意操作。
- `解绑` 现在真正吊销授权。settings 的 `update` 是深合并（`dsh-settings` 的 `mergeLayers` 保留补丁中缺失的键），因此此前所有"删除后 update"的写法都是空操作：`channels` 中的 `ownerUserId` 会留在磁盘上，重新配置同一机器人即恢复旧 owner 并跳过配对。改为用 `replace` 整体写回。
  - **升级提示**：本次修复不会自动清理历史遗留记录。若此前解绑过某个通道且之后没有重新绑定，建议在升级后再执行一次解绑，或检查 `settings.yaml` 中 `qq-weixin.channels` 是否仍有不再使用的条目。

### 修复

- 飞书 / 企业微信的入站回调补上拒绝处理，飞书卡片更新链补上 `onRejected`，飞书长连接握手定时器不再悬空。此前一次瞬时发送失败会成为未捕获拒绝，被 Harness 的全局处理器视为致命错误并退出整个进程。
- 流式回复失败不再静默丢失答案：企业微信 `finish()` 记录队列失败并回退为普通消息，飞书在卡片无法结束时回退为普通消息，Telegram 终态编辑失败时改为补发新消息，长回复余段逐段容错。
- 三个凭据通道的 `start()` 记忆化并加入 generation 守卫。并发启动不再产生重复连接或重复长轮询（Telegram 曾共享游标导致同一消息处理两次，且 `stop()` 只中止较新的循环，旧循环在断开/解绑后仍在消费消息）。
- 企业微信流式上限按 SDK 文档的 20480 字节计算（此前按 UTF-16 字符，中文回答约 6900 字即超限被平台拒绝）；切分与截断不再切断代理对。
- 微信 sidecar：CDN 请求加入超时与中止信号、边读边限量；监控停止的等待加界；空 200 响应加入轮询间隔下限；发送失败同时检查 `ret` 与 `errcode`；`full_url` 仅允许官方 CDN 源；入站文本按帧上限截断；修复 `sleep` 与二维码轮询的监听器泄漏。此前 CDN 卡住会让子进程活着但永久不再响应任何 IPC 帧。
- QQ / 微信子进程异常退出会通知宿主，通道状态不再一直显示"已连接"；扫码绑定加 30 秒超时，`/api/qqbot/begin` 不再永久挂起并锁死设置卡片；原生流式失败改为 5 分钟冷却，不再是进程级永久停用。
- 首次联系时的并发消息不再重复创建同一会话（此前会以"会话已存在"失败并向用户回显"处理失败"）。
- `解绑` 在凭据来自启动环境变量时不再中途中断，清理与状态复位始终执行。QQ / 微信同样改用 best-effort 解除凭据，此前这两个通道仍会因 `unset` 抛错而跳过持久化清理，导致 owner 记录残留。
- 工具审批与提问的消息现在同样经过工作区路径隐藏（此前绕过代理，`reason` 可携带绝对路径发到聊天平台）。
- 无对应待办时，`/同意` `/拒绝` `/answer` 不再被静默吞掉，而是交给 Agent 处理。
- 每消息回复目标的映射表加上容量上限，不再随进程生命周期无界增长；企业微信不再为非 owner 的发送者写入该表。
- 子进程以 `--no-warnings` 启动，且异常退出时才上报 stderr；Node 运行时警告不再被当作插件警告记录。
- `replyTimeoutMs` 增加上界，避免超出 `setTimeout` 范围导致所有任务被立即判为超时。

### 复核修复（对抗性审查发现）

- `start()` 的记忆化不再吞掉一次合法的重连：`stop()` 会清掉备忘录，且清理带身份校验。此前在握手期间点击"断开 → 连接"会复用那次已被作废的启动，旧启动在自己的 generation 检查处返回，于是"连接"报告成功但通道是死的，需要再点一次。
- 外部中止与超时现在覆盖响应体读取阶段。此前 `fetch` 一拿到响应头就释放了定时器与中止监听，读取响应体仍在守卫范围之外，CDN 只发一个分块后挂住依旧会永久阻塞——正是这次要修的那个缺陷。下载改为在守卫范围内边读边限量。
- settings 读改写在本插件内串行化。`update` 合并的是写队列到达时已提交的分区，而 `replace` 原样写入调用方的快照，所以基于旧快照的 `replace` 会静默删掉并发 `update` 刚写入的绑定，反之 `update` 也可能把刚被吊销的键写回来。
- 通道事件监听与轮询循环都按下发时的 generation 记录状态：一次"断开"不会再被随后到达的套接字事件或长轮询成功回包改回"已连接"。
- 企业微信补上握手后的 generation 检查；被作废的启动只清理自己创建的对象，不会误关后来那次连接。
- 重连完成前会再次检查是否已被主动断开，避免"断开"之后通道自行复活并显示已连接。
- 飞书卡片结束逻辑即使此前有更新失败也会尝试关闭卡片，避免卡片永久停留在"正在生成…"。
- `/api/qqbot/begin` 在 `binding.start` 失败时会释放等待中的二维码请求，且提前挂上拒绝兜底，不会产生未捕获拒绝。
- `settingsCache` 在写入成功后才更新，写入失败时仍与磁盘一致。

### 测试

- 新增配对码（熵、有效期、错误次数与轮换）与按字节切分、安全截断的回归测试。
- 新增"握手期间断开后仍能真正重连"以及"被作废的启动不会覆盖新连接"的回归测试。

## 0.5.3

- QQ 原生流式更新或结束失败时改发普通消息交付完整答案，并在当前进程中停用失效的原生流。
- 捕获 Agent 事件回调中流式更新、取消与文本回发的异步失败，避免未处理的 Promise 异常导致 Harness 退出。

## 0.5.2

- 修复 QQ、微信等没有“正在输入”接口的通道收到消息后抛出异常、只回复“处理失败”的问题；输入状态发送失败也不再阻断任务。

## 0.5.1

- 修复设置页使用无效的多段 RPC 通道导致 `invalid RPC target`、无法读取通道状态的问题。
- 设置页按 Harness 的 RPC 格式通过 `/api` 通道调用 `qqbot/*` 接口；安装包验证新增真实 Host 与浏览器模块联通检查。

## 0.5.0

### 安装与发布

- 用户安装流程改为预构建 tgz：不需要克隆、编译或执行插件安装脚本。
- 发布包使用运行文件白名单，排除测试、源码映射、开发脚本和维护文档；产物统一进入 dist/。
- 新增独立消费端安装验证：禁用安装脚本，验证 Host 入口、Web 模块注册及 QQ / 微信子进程握手。
- 新增按版本标签生成 GitHub Release 安装包的工作流。

### Harness 兼容

兼容性基线：DeepSeek Harness 0.1.5-rc.2。

- 兼容目标迁移至 0.1.5-rc.2，更新 Cordis / Schemastery 与 Host 依赖。
- 使用官方 Agent 类型、setup 第二参数、snapshotEvents、分叉继承元数据及新版权限查询。
- 接入 agent/assistant-stream，按 attempt/revision 过滤旧流与重复片段。
- 接入 user-questions/request waterfall，移除 Web 问题必须回浏览器处理的旧限制。
- 移除独立扫码网页和绑定工具；设置页统一使用 Harness 认证的 Fetch 路由。
- 新增真实 Host 安装、Web 启动、接口认证及 Agent 生命周期验证。

### 修复

- 修复 QQ / 微信普通长回复截断，QQ 流式结尾按安全字符边界分段。
- 非 IM 会话的工具审批交给 Harness；IM 会话缺少正确路由时仍拒绝自动放行。
- 设置页捕获连接异常并显示内联错误，新增取消扫码绑定入口。

## 0.4.1

### 修复

- QQ / 微信 Host IPC 在接收数据时限制帧大小，包括没有换行符的异常输出；拒绝空值、错误凭据和畸形 payload。
- 子进程退出与管道错误会清理未完成请求；旧进程的退出事件不再清除新进程。并发停止与重启串行处理，关闭后清理等待计时器。
- 隔离 IPC 事件订阅者的同步异常，防止影响其他订阅者。
- 已取消的审批和问题不会发送；即使发送请求卡住，取消和超时也能结束等待。
- 长消息切分保留 Unicode 代理对，非法长度限制立即报错。
- 更新 qs、js-yaml 与构建工具 esbuild，修复本地依赖审计发现的问题。宿主依赖更新范围见 SECURITY.md。

### 工程化

- 新增异常帧、进程重启、请求超时、问答取消及 emoji 切分回归测试。
- 固定 pnpm 11.19.0，新增 Windows / Linux、Node 22.13 / 24 CI 矩阵与依赖更新配置。
- 新增维护指南、发布流程、故障排查与安全边界说明。

兼容性基线保持 DeepSeek Harness rc.6；本次没有升级 Host API 或 QQ SDK 协议。
